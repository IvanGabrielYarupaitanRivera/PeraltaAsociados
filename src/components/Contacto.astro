---
const formName = "contacto";
---

<section id="contacto" class="py-20 bg-neutral-100">
  <div class="container mx-auto px-4">
    <header class="mb-8 text-center">
      <h1 class="font-serif text-3xl md:text-4xl text-primary font-bold">
        Contáctanos
      </h1>
      <p class="text-gray-600">
        ¿Tienes alguna consulta? Completa el formulario y nos pondremos en
        contacto.
      </p>
    </header>
    <form
      name={formName}
      method="POST"
      data-netlify="true"
      netlify-honeypot="bot-field"
      class="max-w-lg mx-auto space-y-6"
    >
      <!-- Campo oculto para Netlify -->
      <input type="hidden" name="form-name" value={formName} />
      <p class="hidden">
        <label>
          No rellenes este campo si eres humano: <input name="bot-field" />
        </label>
      </p>
      <div>
        <label for="nombre" class="block text-sm font-medium text-gray-700">
          Nombre
        </label>
        <input
          type="text"
          id="nombre"
          name="nombre"
          required
          class="mt-1 block w-full rounded-sm border-gray-300 p-2"
        />
      </div>
      <div>
        <label for="email" class="block text-sm font-medium text-gray-700">
          Correo Electrónico
        </label>
        <input
          type="email"
          id="email"
          name="email"
          required
          class="mt-1 block w-full rounded-sm border-gray-300 p-2"
        />
      </div>
      <div>
        <label for="mensaje" class="block text-sm font-medium text-gray-700">
          Mensaje
        </label>
        <textarea
          id="mensaje"
          name="mensaje"
          rows="5"
          required
          class="mt-1 block w-full rounded-sm border-gray-300 p-2"></textarea>
      </div>
      <div class="text-center">
        <button
          type="submit"
          class="inline-flex items-center px-8 py-2 bg-primary text-white font-medium hover:bg-primary/90 transition-all duration-300 rounded-sm"
        >
          Enviar
        </button>
      </div>
    </form>
  </div>
</section>
