---
import WhatsAppSection from "./WhatsAppSection.astro";
import EmailSection from "./EmailSection.astro";
import LocationSection from "./LocationSection.astro";
---

<section class="py-20 relative bg-neutral-100" id="formas-de-contacto">
  <!-- Patrón de fondo -->
  <div class="absolute inset-0 opacity-30">
    <svg
      width="100%"
      height="100%"
      xmlns="http://www.w3.org/2000/svg"
      aria-hidden="true"
    >
      <pattern
        id="subtle-pattern"
        x="0"
        y="0"
        width="40"
        height="40"
        patternUnits="userSpaceOnUse"
      >
        <path
          d="M0 20h40M20 0v40"
          stroke="#171E60"
          stroke-width="0.5"
          stroke-opacity="0.2"
          fill="none"></path>
        <circle cx="20" cy="20" r="1" fill="#171E60" fill-opacity="0.2"
        ></circle>
      </pattern>
      <rect width="100%" height="100%" fill="url(#subtle-pattern)"></rect>
    </svg>
  </div>

  <!-- Contenido principal -->
  <main class="relative z-10">
    <div class="container mx-auto px-4">
      <!-- Encabezado decorativo -->
      <header class="flex items-center gap-4 mb-8">
        <span class="sm:block h-[2px] flex-1 bg-secondary" aria-hidden="true"
        ></span>
        <p
          class="text-secondary font-medium tracking-wide uppercase text-sm whitespace-nowrap"
        >
          Contacte a su Abogado
        </p>
        <span class="sm:block h-[2px] flex-1 bg-secondary" aria-hidden="true"
        ></span>
      </header>

      <!-- Título principal -->
      <header class="text-center max-w-3xl mx-auto mb-16">
        <h1
          class="font-serif text-3xl md:text-4xl lg:text-5xl text-primary font-bold mb-4"
        >
          Abogado en Huancavelica a su Servicio
        </h1>
        <p class="text-gray-600">
          Elija la forma más conveniente de contactar con su abogado en
          Huancavelica. Respuesta inmediata y primera consulta gratuita.
        </p>
      </header>

      <section class="grid md:grid-cols-2 lg:grid-cols-3 gap-8 relative">
        <WhatsAppSection />
        <EmailSection />
        <LocationSection />
      </section>
    </div>
  </main>
</section>
