---
import { Icon } from "astro-icon/components";
import Layout from "../layouts/Layout.astro";
import "../styles/global.css";
---

<Layout
  title="Página no encontrada | Peralta Asociados"
  description="Lo sentimos, la página que buscas no existe. Encuentra asesoría legal especializada en Huancavelica con Peralta Asociados."
>
  <section class="min-h-screen relative bg-neutral-100">
    <!-- Patrón de fondo -->
    <div class="absolute inset-0 opacity-30">
      <svg
        width="100%"
        height="100%"
        xmlns="http://www.w3.org/2000/svg"
        aria-hidden="true"
      >
        <pattern
          id="subtle-pattern"
          x="0"
          y="0"
          width="40"
          height="40"
          patternUnits="userSpaceOnUse"
        >
          <path
            d="M0 20h40M20 0v40"
            stroke="#171E60"
            stroke-width="0.5"
            stroke-opacity="0.2"
            fill="none"></path>
          <circle cx="20" cy="20" r="1" fill="#171E60" fill-opacity="0.2"
          ></circle>
        </pattern>
        <rect width="100%" height="100%" fill="url(#subtle-pattern)"></rect>
      </svg>
    </div>

    <!-- Contenido -->
    <main
      class="relative z-10 flex flex-col justify-center items-center min-h-screen p-4"
    >
      <article
        class="max-w-lg w-full bg-white p-8 md:p-12 rounded-sm shadow-lg"
        role="alert"
      >
        <!-- Ícono de error -->
        <figure class="mb-8 flex justify-center" aria-hidden="true">
          <div class="rounded-full bg-primary/10 p-4">
            <Icon name="mdi:alert-circle" class="w-16 h-16 text-primary" />
          </div>
        </figure>

        <!-- Mensaje principal -->
        <header class="text-center space-y-4 mb-8">
          <h1 class="font-serif text-3xl md:text-4xl text-primary font-bold">
            Error 404
          </h1>
          <p class="text-gray-600 text-lg">
            Lo sentimos, la página que buscas no existe o ha sido movida.
          </p>
        </header>

        <!-- Separador decorativo -->
        <figure class="my-8 flex items-center gap-4" aria-hidden="true">
          <span class="h-[1px] flex-1 bg-secondary"></span>
          <Icon name="mdi:scale-balance" class="w-6 h-6 text-secondary" />
          <span class="h-[1px] flex-1 bg-secondary"></span>
        </figure>

        <!-- Sección de ayuda -->
        <section class="text-center space-y-4">
          <h2 class="text-gray-600 text-lg">
            ¿Necesitas ayuda legal? Estamos aquí para asistirte.
          </h2>
          <nav class="mt-4" aria-label="Navegación de recuperación">
            <a
              href="/"
              class="inline-flex items-center gap-2 px-6 py-3 bg-primary text-white hover:bg-primary/90 font-medium transition-all duration-300 rounded-sm group"
              rel="home"
            >
              <Icon
                name="mdi:arrow-left"
                class="w-5 h-5 transition-transform group-hover:-translate-x-1"
                aria-hidden="true"
              />
              <span>Volver al inicio</span>
            </a>
          </nav>
        </section>
      </article>
    </main>
  </section>
</Layout>
